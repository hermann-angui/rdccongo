<nav class="navbar navbar-expand-lg navbar-light p-0">
    <!--   <a class="navbar-brand" href="#">Navbar</a> -->
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse  justify-content-between" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item">
                <a class="nav-link" href="{{ path('app_home') }}">{{ 'home' | trans }} </a>
            </li>
            <li class="dropdown nav-item">
                <a class="nav-link dropdown-toggle" href="{{ path('app_home') }}" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    {{ 'visa' | trans }}
                </a>
                <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                    <a class="dropdown-item" href="{{ path('app_application_new') }}">Apply for a Visa</a>
                    <a class="dropdown-item" href="{{ path('app_application_check_eligibility') }}">Check Visa Eligibility</a>
                    <a class="dropdown-item" href="{{ path('app_application_check_status') }}">Check Application Status</a>
                    <a class="dropdown-item" href="{{ path('app_application_check_status') }}">Visa Data Correction</a>
                    <a class="dropdown-item" href="{{ path('app_application_pay') }}">Pay For Application</a>
                </div>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('app_about_us') }}">{{ 'about_us' | trans }} </a>
            </li>
        </ul>
        {% if app.user is null  %}
            <a class="btn btn-sm btn-dark rounded-5 mx-2 px-4" href="{{ path('app_login') }}">{{ 'sign_in' | trans }}</a>
        {% else  %}
            <a class="btn btn-sm btn-dark rounded-5 mx-2 px-4" href="{{ path('app_logout') }}">{{ 'logout' | trans }}</a>
        {% endif  %}
    </div>
</nav>